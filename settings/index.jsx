// @ts-check
import * as React from "react";
import { FormattedMessage } from "react-intl";

/**
 * 
 * @returns {JSX.Element}
 */
export default function Settings() {
  return (
    <div>
      <FormattedMessage
        defaultMessage="Settings"
        description="settings folder"
      />
    </div>
  );
}
